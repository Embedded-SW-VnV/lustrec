; (rule
;  (alias runtest)
;  (deps
;   (:scr test.sh)
;   (:lus
;    (glob_files *.lus)))
;  (action
;   (run %{scr} %{bin:lustrec} "%{lus}")))

(test
 (name test)
 (libraries re unix)
 (deps
  (:lus (glob_files *.lus))
  (:ign ignored))
 (action (run %{test} %{bin:lustrec} "%{lus}" %{ign})))
